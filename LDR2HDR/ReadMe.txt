This is a C++ application to convert a serie of LDR images taken from multiple exposures into a single HDR image. 

To compile it, one will need the OpenCV and Eigen libraries.

To run it, use the following arguments: 
	config.txt: configuration file which indicates where are the LDR images and the exposure times;
	output.hdr: name of the output HDR image